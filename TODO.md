# TODO — Radiance 26.2 port

Feature roadmap for the MC 1.21.x → 26.2 port. This is the shared source-of-truth ordering;
the native-side counterparts live in `../MCVR/TODO.md`. Diff against the `main` branch to see
how each feature worked in 1.21.x.

**State:** ✅ done · ⚠️ wired but broken/wrong · ❌ not implemented (no-op stub) · 🔁 regressed · 🐞 crash

## Order of attack

| # | Feature | State | Where | Notes |
|---|---------|-------|-------|-------|
| 1 | Terrain visible while walking | ✅ (confirm) | `WorldRendererMixins` | Fixed: `render` is cancelled, so 26.2's per-frame `viewArea.repositionCamera` (moved into `LevelRenderer.render`) was never called → grid froze at load point. Now replicated. Awaiting user confirm. |
| 2 | **Mobs render correctly (not red)** | ✅ (confirm) | `EntityProxy.resolveTextureGlId`, `StorageVertexConsumerProvider.resolveTextureId` | Fixed: texture id was resolved as "first GL-backed sampler", but `RenderSetup.prepareTextures` orders the list [overlay(Sampler1), lightmap(Sampler2), main(Sampler0)] — so mobs sampled the red/white **overlay** as albedo. Now selects `Sampler0` by name. Should also fix block-entities/items/**hand**. Awaiting user confirm. |
| 3 | **GUI item icons** (hotbar, inventory) + inventory player model | ✅ | Radiance `RenderTargets`/`TextureUtilMixins`/`CommandEncoderMixins`/`RenderPassMixins`/`ShaderProxy` + MCVR `textures`/`ui_module` RTT + JNI | **DONE (user-confirmed 2026-08-06): icons render, inventory player model renders.** 26.2 renders GUI items into a `GuiItemAtlas` RTT (and inventory/skin/banner previews via `PictureInPictureRenderer`), both redirected by `RenderSystem.outputColorTextureOverride`; the mod routed everything to the single overlay → empty targets. Fix = render-target awareness (option A): import `USAGE_RENDER_ATTACHMENT` textures as color-attachment+sampled; draws whose active target is a registered RTT texture route to a native RTT pass (`beginTarget`/`drawToTarget`/`endTarget`) reusing the overlay pipelines (compatible RGBA8_UNORM+D32_SFLOAT). Key fixes across rounds: depth image init/clear (reverse-Z 0.0 far), mixin `<clinit>` startup NPE (`be15b92`), and the **Y-flip** — RTT renders with a negative-height viewport so targets store GL-bottom-up for MC's fixed V-flipped blit; cull off since the flip inverts winding (MCVR `3e5f6d2` + Radiance `123d8a3`). Remaining minor follow-up: animated item slots (clock/compass/buckets) may need their captured per-slot clear/scissor rect Y-flipped from MC (GL) to Vulkan coords. |
| 4 | **Glowing / emissive tiles** (lava, torches, glowstone, fire) | ✅ — part A + B done, user-confirmed | `BlockModelRendererMixins`, `PBRVertexConsumer`, `CommandEncoderMixins`; native `chunks.cpp`/`emission.cpp` | **Root cause:** emissive block *lights* only ever came from **LabPBR specular** emission cells (`AuxiliaryTextures.loadAndUpload` → `EmissionRecorder` → native R-tree → `buildLightInfos.collectCells`). 1.21.x drove `loadAndUpload` from `NativeImage.uploadInternal`; 26.2 removed that route and the port **never re-attached it** (moved only the albedo upload to `CommandEncoderMixins`), so `loadAndUpload` is orphaned → no cells → nothing emits. (The tint-provider emission path is ~always 0: no `BlockColorEmissionProvider` is registered, same in both branches.) **Part B DONE (Radiance bedae06 + MCVR 9839754):** vanilla fallback — `setBlockEmission` feeds `state.getLightEmission()/15` into the per-vertex `albedoEmission`, and native `buildLightInfos` synthesizes a whole-quad area light when no LabPBR cell covers the quad → emissive blocks light the scene **without a PBR pack**. **Part A DONE (Radiance d398bef) — user-confirmed working with a LabPBR pack 2026-08-14.** Answer to the open question: 26.2 uploads block/item sprites **per-sprite** — MC stitches the atlas via render-to-texture, which the mod already CPU-mirrors in `TextureAtlasMixins.radiance$uploadSpritesToVulkan`. So the aux re-wire belongs THERE (not the `writeToTexture` hook the note guessed). Per sprite: convert `contents().name()` (sprite id) → source resource id via `SpriteSource.TEXTURE_ID_CONVERTER`, then `loadAndUpload` the `_s`/`_n`/`_f` maps + decode emission at the sprite's stitched offset (gated to block/item/entity paths). Refactored `loadAndUpload` to take `targetId`+`identifier` explicitly (source `targetID` is never set in 26.2) and fixed a latent NPE (aux atlas wasn't registered in `GLID2Texture`). No native change — the base→specular/normal/flag mapping (`BufferProxy.updateMapping`) + shader consumption (`convertLabPBRMaterial`→`mat.emission`; advanced area-light cells) were already wired. **Verify with a LabPBR pack** (present on the Windows box): pack-authored emissive blocks glow + PBR materials apply. **Follow-ups:** standalone/entity textures via the `writeToTexture` hook; per-tick animated-sprite aux frames. |
| 5 | **Animated textures** (water/lava/fire/portal flow) | ✅ | `TextureAtlasMixins` + `SpriteAnimationMirror` | **DONE (user-confirmed 2026-08-06).** 26.2 advances atlas animations by *rendering* the current frame into the atlas (`TextureAtlas.uploadAnimationFrames` → `AnimationState.drawToAtlas`), an un-replayed render-to-texture → Vulkan atlas kept frame 0. Fix (Radiance c7a0e02): inject at `uploadAnimationFrames` HEAD and CPU-copy the current frame's sub-region into the atlas cell via `queueUpload` (same path as the first-frame static fix, just a non-zero source offset = the frame's position in the sprite strip). Frame index/layout read via cached reflection (`SpriteAnimationMirror`) since they live on MC's package-private `SpriteContents$AnimationState`/`$AnimatedTexture`/`$FrameInfo`. Per-sprite last-frame cache avoids redundant uploads. Smooth cross-fade tracked as **#17**. |
| 6 | **Block-targeting highlight wireframe** | ⏸️ deferred | native `entities.cpp` `DrawMode::LINES`; `EntityProxy.queueTargetBlockOutlineRebuild` | **PAUSED (user 2026-08-06): distracting wrong lines are gone, no outline currently drawn — acceptable for now; revisit later once desired look is decided.** Progress: outline was an open "L", then extended E-W. Two real bugs found & fixed: (1) native LINES expansion reshuffled 4→6 verts fabricating a spurious diagonal (fixed 33c7e37: pair GL_LINES verts directly + tube each edge); (2) 26.2 `lines()` is `POSITION_COLOR_NORMAL_LINE_WIDTH` (24-byte stride, extra per-vertex R32F line width) — native read 20-byte stride → drift. Widening the shared `PositionColorNormal` struct to fix (2) **broke terrain** (it also backs format-5's pipeline layout / shared.hpp is compiled into shaders); reverted + localized the 24-byte stride to the LINES parser (2995391) — **terrain restored (confirmed the shared-struct widening was the culprit)**, but now no outline renders (thin tubes hug the edges / not visible). **Next when resumed:** verify the tubes render at all; if too thin/z-fighting, bump `task.lineWidth` (currently 0.0075) or offset the tubes off the block face. Commits: MCVR 33c7e37 + 2995391 (native-only). |
| 7 | **Sun & moon discs** | ✅ (moon size deferred) | `WorldRendererMixins:196-208`; native `world_miss_eval.glsl` (+`_impl`) | **DONE — sun & moon render (user-confirmed 2026-08-07).** 26.2 stitches the sun + each moon phase into a shared `celestials` TextureAtlas (SkyRenderer) — no standalone sun/moon GL texture. But the source PNGs still ship (`textures/environment/celestial/sun.png`, `.../moon/<phase>.png`) and `TextureManager.getTexture(Identifier)` lazily registers+loads any path as its own full-extent `SimpleTexture` — the same route `END_SKY_LOCATION` already uses. So load the sun and the **current phase's** moon disc as standalone textures and feed their GL ids to the existing `skyUBO.sunTextureID/moonTextureID` (plumbing already present). Two changes only: (a) Java resolves the two textures (moon path = `.../moon/` + `MoonPhase.getSerializedName()` + `.png`, since the old 4x2 `moon_phases.png` grid was split into 8 per-phase files); (b) native `evalMoonBillboard` now samples the moon **full-extent** instead of indexing a 4x2 tile (`skyUBO.moonPhase` no longer used by shaders). No UBO/BufferProxy/native-struct change. **Deferred follow-up (user 2026-08-07): moon looks a bit small** — bump the moon billboard angular size in `evalMoonBillboard` (`tanHalf = tan(0.05)`; the per-phase PNG likely has more transparent margin than the old atlas tile, shrinking the visible disc) once the exact size is dialed in. Commits Radiance 135ca1a + MCVR 0c44b2c. |
| 9 | **Fog** (shape + sky-dark are N/A in 26.2; env near-fog deferred) | ⏸️ deferred | `WorldRendererMixins:173`; native shaders read only `worldUBO.fogStart/fogEnd/fogColor` | **Investigated 2026-08-07 — the two originally-named fixes are NO-OPS in 26.2, so deferred to the one real improvement (Option A).** (1) **Fog shape: gone.** 26.2 deleted the `FogShape` concept — `FogData` has no shape field (only `environmentalStart/End`, `renderDistanceStart/End`, `skyEnd`, `cloudEnd`, `color`), and **no shader reads `fogType`** anyway; the RT applies fog by radial ray distance, which already matches 26.2's shapeless distance fog. The hardcoded SPHERE(0) is correct + the UBO field is dead. (2) **sky-dark: dead field.** Zero shader references to `isSkyDark`; hardcoding `false` has no effect (the RT blacks out void/End via `skyType`). **Deferred real work (Option A, user 2026-08-07):** the mod feeds only `fogData.renderDistanceStart/End` (far view-distance fog) and ignores `environmentalStart/End` — the dense NEAR fog for **Nether / underwater / lava / powder snow / blindness+darkness**. Wire a denser-wins combine of the two ranges (Java-only, no shader change) so those environments fog correctly. Small; touches overworld fog so verify no over-fog regression. |
| 10 | **Particles** (block break, smoke, crits…) | ✅ | `EntityProxy.queueParticleRebuild` | **DONE — particles render (user-confirmed 2026-08-09).** 26.2 removed `Particle.render(VertexConsumer,…)`; particles `extract` into render states drawn by `QuadParticleFeatureRenderer` (a bare `FeatureRenderer` writing a GPU `StagedVertexBuffer` directly) → the mod's `RenderTypeFeatureRenderer` capture hook never saw them, and the stub had no caller. Re-implemented against the extract API: `ParticleEngine.extract` fills a `ParticlesRenderState` whose **sole** concrete group is `QuadParticleRenderState`; `buildLayer(Layer, VertexConsumer)` re-emits each layer's quads into a `PBRVertexConsumer`, keyed on a synthesized `RenderTypes.entityCutout/entityTranslucent(layer.textureAtlasLocation())`. **First tried the native post-render particle pass** (`Target::Particle`) — but staged probes proved the post-render stage's output **never composites to the presented frame in 26.2** (capture perfect, pass ran + drew, yet a forced-opaque-nearest-no-discard fragment was still invisible; `fuseWorld` presents the upscaler's `sharedImages_[0]`, not PostRender's `out:ldr`). **So route particles through the proven world-RT path instead** (like clouds/entities): `processWorldEntityRenderData(RayTracingFlags.WORLD)` at entity origin = `camera.position()` (so the native WORLD transform `entityPos−cameraPos=0` keeps the camera-relative quads in place) + `queueBuild(Coordinates.WORLD)`. Commits: caller wired (Radiance d1e4787), world-RT switch (Radiance 6da115b + MCVR b7eb166 revert of probes). Also fixed a **startup device-lost crash** surfaced this round: atlas sprite uploads to a nonexistent mip level (26.2 single-mip imported atlas vs MC's higher `mipLevelCount`) faulted the GPU — guarded natively (MCVR 03ece0e) + Java clamp (Radiance 35d4b43/e265a43). **Follow-ups:** particles are now ray-traced (cutout transparency not smooth alpha, scene-lit not self-emissive); the cleaner screen-space look needs the PostRender output wired into the presented image (a real post-compositing regression that also blocks weather/text/name-tag/star/DOF — deferred). |
| 11 | **Weather** (rain/snow) | ✅ | `EntityProxy.queueWeatherBuild` + `WeatherEffectRendererMixins` (@Invoker); `WorldRendererMixins` caller | **DONE — rain/snow render (user-confirmed 2026-08-09). Java-only.** 26.2 renders precipitation via `WeatherEffectRenderer`: `extractRenderState(level, tickDelta, cameraPos, WeatherRenderState)` collects the visible `rainColumns`/`snowColumns` (public `ColumnInstance` records + `intensity`/`radius`), then `render()` tessellates them (camera-relative quads) via the **private** `renderInstances(VertexConsumer, columns, cameraPos, vScale, radius, intensity)` into a GPU buffer the backend never sees; the stub also had no caller. Implemented like #10 particles: `extractRenderState` → drive MC's own `renderInstances` into a `PBRVertexConsumer` via a new `@Invoker` (`WeatherEffectRendererMixins`) — reusing the rain-table column math — with `render()`'s factors (rain 1.0 / snow 0.8) and the standalone `textures/environment/rain.png`/`snow.png` (touched via `TextureManager.getTexture` so they import before the render type resolves their GL id), keyed on `RenderTypes.entityTranslucent(tex)`. Injected through the **world-RT** path (`RayTracingFlags.WORLD` at entity origin = `camera.position()` so the native WORLD transform `entityPos−cameraPos=0` keeps the camera-relative quads in place) — NOT the post-render weather pass, which shares the #10 post-compositing dead end. Caller wired in `WorldRendererMixins` next to particles. **Follow-ups:** ray-traced look (cutout not smooth-alpha streaks, scene-lit); world-border wall not handled (dropped the old `WorldBorderRenderer` param). |
| 12 | **Item glint** (enchantment shimmer) | ✅ | `ItemFeatureRendererMixins` + `GlintContext` + `WorldRendererMixins:154` | **DONE — user-confirmed 2026-08-14 (glint + 3D depth work on held weapons); fixes #15 too. Only worn-armor glint remains (see note).** 0cf866b (supersedes fold c991422). 26.2 draws item glint as a SEPARATE `prepareFoilSubmit` pass (distinct from `prepareMainSubmit`); folding it doubles the geometry → coplanar z-fight → flat item (the c991422 regression). CURRENT: stamp glint onto the item's OWN main-pass vertices via a thread-local (`GlintContext`, read by `PBRVertexConsumer.beginVertex`/`setUv`) when `submit.foilType() != NONE`, and cancel the separate foil pass — one geometry copy, full depth, no rectangle. Plus `WorldRendererMixins` classic scroll matrix (`setupGlintTexturing(0.16F)` value) → `worldUBO.textureMat`. Worn-armor glint (`ENCHANTED_GLINT_ARMOR`, a different feature renderer) not yet handled. If item is now 3D but glint invisible, suspect the glint PNG isn't in native's bindless array (like #7 sun/moon). |
| 13 | **Screenshot / world-icon capture** | ❌ | `GameRendererMixins:43` | `renderLevel` capture is a TODO. |
| 14 | **World shader translation** (terrain/clouds/end_portal) | ⚠️ | shader translator | 4 world shaders fail translation (new 26.2 ChunkSection block fields absent from BuiltinUniforms). **Low priority** — world is RT'd from captured meshes, not these draws. |
| 15 | **First-person held item: enchantment glint renders as a flat square** | ✅ | folded into **#12** (`ItemFeatureRendererMixins`) | **DONE — user-confirmed 2026-08-14 (held weapons render 3D + glint).** Root cause was the separate opaque glint geometry captured by the generic feature hook (`getVertexBuilder(glint)` via `getFoilBuffer`); now folded into the item as a per-vertex glint overlay and the separate geometry is suppressed. See #12. |
| 16 | **Handheld light sources emit light** (torch/lantern/glowstone in hand) | ✅ (user-confirmed 2026-08-07) | `WorldRendererMixins` + `BufferProxy` (SkyUBO); native `shared.hpp` + vanilla-pt `dynamic_point_light.glsl`/`shadow.rahit`/`default.rchit`/`no_height.rchit` | **Fix applied (Option A — analytic point light), awaiting user confirm.** Chose A over ReSTIR chunk-injection (B) after finding 1.21 had NO handheld/dynamic light and vanilla-pt (the active pack) does **no NEE for local lights** — emissive blocks only glow via path-traced hits — so a clean carried-torch needs its own sampled point light + shadow ray. **How:** a fixed array (≤4) of `DynamicLight{pos,intensity,color,range}` appended to `SkyUBO` (reuses `pad0`→`dynamicLightCount`; buffer auto-grows via `sizeof`, **no** new descriptor/binding/JNI/buffer). Java feeds one light at the player's eye (camera-relative scene space) from the max block-light level of main/off hand (`Block.byItem(...).defaultBlockState().getLightEmission()`), warm tint, intensity ~ `(level/15)^2 * 10`, reach = level blocks. Native `sampleSurfaceDynamicPointLights` (in `default.rchit`+`no_height.rchit`) does inverse-square NEE with a shadow ray, added into `directLight` (denoised like the sun). `shadow.rahit` zeroes `throughput` on opaque so the point light reads it as pure visibility (invisible to the sun, which uses `radiance`). Commits Radiance bc4beb1 + MCVR d319007. **Tuning knobs if wrong on test:** `RADIANCE_HANDHELD_LIGHT_GAIN` (brightness), the warm tint, and the eye Y-offset. Only vanilla-pt wired; advanced pack unaffected. Extends trivially to other entities (more slots). |
| 17 | **Animated-texture interpolation** (smooth cross-fade) | ⚠️ | `TextureAtlasMixins.radiance$mirrorAnimatedFrames` + `SpriteAnimationMirror` | **Low priority** (user-deprioritised 2026-08-06). #5 mirrors the current *discrete* frame into the Vulkan atlas each tick, so interpolated sprites (default water/lava) step between frames instead of MC's GPU cross-fade. 1.21 computed interpolated frames on the CPU (`SpriteContents.Ticker.generateInterpolatedFrame`: lerp current↔next frame by `subFrame/frameTime`) and uploaded those; replicate that — read `subFrame` + the next frame's grid index (already reflected via `SpriteAnimationMirror`), blend the two frames into a scratch buffer, `queueUpload` it. Only for sprites whose animation metadata sets `interpolate`. |
| 18 | **Sign text renders as black rectangles** | ✅ | `StorageVertexConsumerProvider.resolveAlphaMode`; native vanilla-pt `configs.json` hit groups | **DONE — user-confirmed 2026-08-14. Fix: Radiance 63e4d6d + MCVR 451f40c.** Sign text is block-entity geometry on the world-RT path; the RT text hit group (`text.rahit`, selected by render type name) carves glyph coverage from a channel chosen by the per-vertex **post-text mode** (`resolveTextTextureColor`: INTENSITY→`.r`, RGBA→`.a`). Two bugs: (1) `resolveAlphaMode` returned plain `TRANSPARENT(2)` for every text type → `text.rahit` read 2 = INTENSITY → sampled `.r`, opaque everywhere for an RGBA font (`text`/`text_polygon_offset` = signs) → whole glyph cell survived → black rectangle. Fixed by deferring to `PBRVertexConsumer.getPostTextMode(name)` (name is authoritative; 4-bit alphaMode field holds ≤8). (2) The vanilla-pt hit-group map only had see-through/polygon-offset text variants, so a plain `text`/`text_intensity` name fell to `default.rahit` (which only ever carves on `.a`). Added `text`/`text_intensity` hit groups (→ `text.rahit`). **Needs native+shader rebuild** (config change). Follow-up if still wrong: confirm the exact sign render type name + font channel. **Reported 2026-08-09.** Sign glyphs show as solid black rectangles (the full glyph cell), not the letter shapes. Sign text is block-entity geometry captured via the world-RT drain (a `TextFeatureRenderer` in the BE render), NOT the dead post-render text pass — so the geometry reaches the screen but the **glyph alpha / text-intensity mode isn't applied**: the font-atlas glyph's alpha (which carves the letter out of the cell) is ignored → the whole quad is opaque, and the sign's dark text color makes it black. Native `text.rahit` exists; the fix is likely making the RT text hit read the glyph alpha + the POST_TEXT_MODE (background/intensity/rgba, see `PBRVertexConsumer.getPostTextMode`) so intensity-mode glyphs use the sampled alpha as coverage and don't fill the cell. Investigate: is the text render type's `textureID`/`alphaMode` resolved (Sampler0 = font atlas) and is the RT anyhit discarding sub-threshold-alpha texels? Not yet started. |
| 19 | **ShaderPackScreen text invisible** (title + list names) | ✅ | `client/gui/ShaderPackScreen.java` text colors | **DONE — text renders (user-confirmed 2026-08-09). Java-only.** ROOT CAUSE (via `javap`, not scissor/stratum/text-path — all those red herrings ruled out): 26.2 `GuiGraphicsExtractor.text` **early-returns when `ARGB.alpha(color) == 0`** (pre-26.2 MC force-opaqued a zero-alpha RGB color; 26.2 skips it). `ShaderPackScreen` was the ONLY mod screen still passing RGB-only colors (`0xFFFFFF`/`0xB0B0B0`/`0x9A9A9A`, alpha byte 0) → its title + shader-name entries drew nothing; button labels + every other mod screen already pass `0xFF..` → rendered (that's why only this window was blank). Fix: opaque alpha byte on the 4 colors. **LESSON: 26.2 GUI text needs an explicit non-zero alpha byte (ARGB, not RGB) — `0xRRGGBB` renders nothing (`GuiGraphicsExtractor.text` early-returns).** (The scissor/stratum/text-path theories chased earlier were all red herrings.) |
| 20 | **Torch/warm block glow is white, not yellow** | ✅ | native `chunks.cpp buildLightInfos` + `PBRVertex.emissionColor`; Radiance `EmissiveBlockColor`/`BlockEmissionContext`/`BlockModelRendererMixins`/`FluidRendererMixins` | **DONE — user-confirmed 2026-08-14 (torches look good). Option b (texture-derived); MCVR af342e8 + Radiance 5aae0c6.** The vanilla-fallback area light used `avgTint × quadEmission` (vertex color-layer ≈ white). Now Java computes each emissive block's representative color (`EmissiveBlockColor`: luminance-weighted avg of the sprite's opaque texels, normalized to max=1 so it carries hue not brightness — a torch's flame beats its handle), passes it per-quad via a new build-only `emissionColor` PBR vertex field (128→144B, guarded by the loud stride asserts on both sides), and native tints the synthesized light with it (`emitColor × avgTint × quadEmission`; falls back to `avgTint` when unset). Torch/lava/redstone → warm; soul torch/sea lantern → true cool color; modded emitters work (texture-driven). **Needs native+Java build.** Follow-up if a torch still reads dull: bias the luminance weight harder (lum²) so the flame dominates more. |
| 21 | **Held torch (first-person) doesn't glow** | ✅ | `ItemFeatureRendererMixins` (`prepareMainSubmit`) + `BlockEmissionContext`; relates to #4, #16 | **DONE — user-confirmed 2026-08-14 (held torch is emissive). Fix Radiance a6ad156, Java-only.** Item geometry never passed through the block emission hook, so a held torch's flame got no `albedoEmission`. Item quads ARE written via `PBRVertexConsumer` at `ItemFeatureRenderer.prepareMainSubmit` during the hand-capture drain (not `renderItem`, which only submits nodes) → set `BlockEmissionContext` there (HEAD sets, RETURN clears, bracketing this item's writes) from `Block.byItem(stack).defaultBlockState().getLightEmission()/15` (the #16 pattern). RT self-glows it (`default`/`no_height`/`world_no_reflect` `.rchit` already do `radiance += tint × albedoEmission`). Scoped to `FIRST_PERSON_{LEFT,RIGHT}_HAND` via `submit.displayContext()` + the local player's hand (via `getMainArm`), so the arm/GUI/third-person are untouched. **Java-only.** Follow-ups: third-person/dropped/item-frame torches; tune strength if dim (currently level/15). |
| 22 | **Lantern emission is per-face-wrong (copper/new lanterns)** | ❌ | self-glow is albedo-modulated (`default.rchit`: `emissionRadiance += tint × albedoEmission`) + per-quad emission; relates to #4/#20 | **Reported 2026-08-14. UPDATED: still wrong with ALL resource packs removed → NOT a LabPBR/aux issue.** Copper lantern (+ new variants) emit from the **top** (wrong) not the **body/flame** (wrong); vanilla `lantern`/`soul_lantern` are correct. Ruled out: (1) pack/aux mapping (no-pack still wrong); (2) a missing hook — verified 26.2 `ModelBlockRenderer.tesselateAmbientOcclusion` **and** `tesselateFlat` both funnel through the hooked `putQuadWithTint`, so **every** normal block quad already gets `albedoEmission = lightLevel/15` uniformly. **Leading hypothesis:** the glow is uniform but the *visible* self-glow is **albedo-modulated** — `default.rchit` does `emissionRadiance += currentSurface.tint × albedoEmission` where `tint` is the sampled albedo, so a **dark** metal lantern-body texture glows dark while the **light** top-cap texture glows bright, reading as "top emissive, body not". The actually-bright flame is a small/occluded area inside the cage. Fix direction: for vanilla-emissive quads, decouple self-glow brightness from albedo (e.g. floor it, or use the #20 `emissionColor` hue at the light-level intensity) so a dark emissive surface still reads as glowing — but this touches ALL emissive blocks, so verify it doesn't wash out lava/glowstone. NOTE (user 2026-08-14): **when working this, re-check EVERY potential light source both WITH and WITHOUT a resource pack** (the two emission paths — LabPBR `mat.emission`/aux cells vs vanilla `albedoEmission` fallback — behave differently; a fix for one can regress the other). Pre-existing, not a #20 regression. |
| 23 | **Handheld light (#16) is too faint / short-range** | 🔧 | `WorldRendererMixins:99` gain + `dynamicLights[7]` range + `dynamic_point_light.glsl` window | **Iterating; brightness user-confirmed OK 2026-08-15, range still being dialed.** Current state: **gain 60** (near-field, was 10→30→60; the linear falloff maxes at 1 near source vs old inverse-square ~4, so needs higher gain — user says brightness fine); **falloff `1 - (d/R)²`** (parabolic window: ~1 near source + stays high through mid-range + eases to 0 at cap; replaced `(1-d/R)²` which crushed mid-range to ¼ at half-radius, and the earlier inverse-square that hotspotted); **range cap = block level × 2** (torch → 28, via `dynamicLights[7]`, was 1×→1.5×→2×). Commits: gain e1c803e/…/60; falloff 9982e2b→20366af; cap 84d1a72→2a25698. **Fix applied to BOTH packs (MCVR df1ae70), awaiting confirm.** Current: gain 60, falloff `1-(d/R)⁴`, cap ×2.5. **ROOT CAUSE (2026-08-15): the user runs the "Advanced" shader pack, but #16's handheld point light was only wired into "vanilla-pt".** So the Java gain (pack-agnostic SkyUBO value) changed brightness, but every falloff/range shader edit did nothing under Advanced — for many rounds. Fix: copied `dynamic_point_light.glsl` into `advanced/common/` and added the point light to `advanced/direct_light/direct_light.rgen` (`directRadiance = areaLight + directionalLight + handheldLight`) — Advanced computes PRIMARY direct light in the ReSTIR resolve, not the rchit. **LESSON: there are two built-in packs (`vanilla-pt`, `advanced`); confirm which is active; a shader edit to one does NOTHING to the other. See [[handheld-shader-two-packs-lesson]].** |
| 25 | **Flickering torches** (enhancement) | 💡 later | `WorldRendererMixins` handheld light intensity (easy); placed emissive area lights (harder) | **Enhancement (user 2026-08-15).** Torch light should waver/flicker like a real flame instead of a steady glow. Easy first pass: modulate the handheld dynamic-light `intensity` (and maybe color temp) by a cheap time-based noise/sine per frame in `WorldRendererMixins` (Java-only, `Util.getMillis`). Harder/later: flicker PLACED torch/fire block lights too (their emission is baked per-quad into chunk area lights — would need per-frame modulation of the emissive contribution or a time term in the shader). Keep subtle. |
| 24 | **Handheld light disappears when too close to a surface** | ❌ | `dynamic_point_light.glsl` shadow ray / NoL (a geometry effect near the light) | **RULED OUT: source-in-block (probe logged ZERO lines) AND temporal decay (user retested 2026-08-14: it does NOT fade over time).** Remaining symptom: the usable band is thin — too far = too dim (that's #23 brightness), and **too close to a surface = the light disappears**. This can't be attenuation: the native falloff is now linear `(1-d/R)²`, which is BRIGHTEST at d=0 — so a very-close surface should be max-lit, not gone. So "disappears when close" is a shadow-ray / NoL / geometry effect specific to the local point light when a surface is right next to it (candidates: `NoL<=0` cull as the light nears coplanar; shadow-ray self-occlusion at tiny distance; the shadowLen/offset interplay). **NEXT: shader bisect** — force `visibility = vec3(1.0)` (and skip the NoL cull) for the handheld light in a probe build; if close surfaces then stay lit, it's the shadow ray, else it's NoL/geometry. Separate from #23 brightness. |
| 24b | ~~Handheld light cuts out near a wall (source in block)~~ | ✅ ruled out | — | Folded into #24; probe disproved source-in-block. | **Reported 2026-08-14; happens specifically when FACING INTO a wall up close.** The carried-torch light goes completely out. **RULED OUT:** (a) the first-person arm — it's `HAND_MASK`, and the handheld-light shadow ray masks `WORLD_MASK\|PLAYER_MASK` only, so the arm can't occlude; (b) own body — `PLAYER_MASK` geometry generally isn't in the TLAS in first person. Static geometry analysis says a faced wall SHOULD stay lit (light sits at `eyePos-(0,0.4,0)` = camera-relative `(0,-0.4,0)`, in the player's air cell ≥0.3 from the wall face; shadow rays from the wall to the light go into the room, unoccluded; NoL>0; atten>0). So the cause is non-obvious — a real position/normal/shadow-ray data bug, not simple geometry. **PROBE IN PLACE (Radiance 407db38):** run with env `RADIANCE_PROBE_HANDHELD=1`, face the wall until the light cuts out, read `latest.log` for `[HandheldProbe] source-in-block:` lines. If they appear during the cutout → source embeds in a block (fix: clip the light out of solids via `level.clip`, or reposition). If the log is SILENT during the cutout → source is NOT in a block → it's the shader shadow ray / NoL (add a shader bisect next: force `visibility=1` for the handheld light and see if the wall lights up). Independent of the #23 range/brightness work. Strip the probe after. |

## Stability (slot in whenever it annoys)

- 🐞 **Clean exit** — "normal crash on exit" every run; shutdown-order teardown.

## Done this port (recent)

- ✅ Menu panorama / sky cubemap (#8) — user-confirmed working 2026-08-06 (regression cleared once Iris was removed).
- ✅ Intermittent corrupt-TLAS crash (~10s–2min in-world) — native barrier fix (MCVR).
- ✅ Invisible terrain after walking (#1).
- ✅ Compositing, world flip (reverse-Z), black sky, black terrain lighting, buttons, text.

See `MIGRATION-26.2.md` for the full migration log.
